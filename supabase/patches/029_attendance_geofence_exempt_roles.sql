-- ============================================================================
-- 029: פטור מבדיקת רדיוס לפי סוג משתמש
-- המנהל יכול לבחור אילו תפקידים (user_role) לא חייבים להיות בקרבת העסק לכניסה.
-- ============================================================================

alter table public.businesses
  add column if not exists attendance_geofence_exempt_roles public.user_role[] not null default '{}';
