-- ============================================================================
-- 027: סוג הסכם נוסף — טופס 101 אישי לכל עובד (PDF לחתימה)
-- הרצה: Supabase Dashboard -> SQL Editor -> Run (לאחר 026)
--
-- מוסיף ערך לאנום agreement_type כדי שהמנהל יוכל להעלות טופס 101 ייחודי
-- לכל עובד, עם תיבות חתימה, בדיוק כמו הסכם אישי.
-- הערה: ALTER TYPE ... ADD VALUE חייב לרוץ מחוץ לטרנזקציה — מריצים את
-- הקובץ הזה בנפרד (לבדו) ב-SQL Editor.
-- ============================================================================

alter type public.agreement_type add value if not exists 'form_101';
